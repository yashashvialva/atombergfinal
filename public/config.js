// Configuration for the Video Support Platform frontend.
// By default, if BACKEND_URL is empty, it will use the current domain.
// In production (when hosted on Vercel), set this to your Railway app's public URL, e.g., "https://atombergfinal-production.up.railway.app"
window.APP_CONFIG = {
  BACKEND_URL: "https://atombergfinal-production.up.railway.app"
};
